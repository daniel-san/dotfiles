super + shift + w
    brave

super + v
    $TERMINAL -e /home/daniel/.config/vifm/scripts/vifmrun

super + F1
    $TERMINAL -e pulsemixer

super + shift + l
    lutris

#### Keypad binds ####
# keypad keys use a implicit 'shift', so KP_1 is actually (shift + KP_1)
super + KP_0
    $HOME/Apps/sublime_merge/sublime_merge

